/** Nome do produto na UI, e-mails e páginas legais. */

export const NOME_PRODUTO = "Orion Lead Hunter" as const;

/** Título curto pra sidebar / login (duas linhas visuais). */
export const NOME_PRODUTO_PARTES = {
  primaria: "Orion",
  secundaria: "Lead Hunter",
} as const;
